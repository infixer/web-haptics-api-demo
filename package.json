{
  "name": "haptics-morse-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run build -w @infixer/polyfill-web-haptics-api && npm run dev -w web",
    "dev:polyfill": "npm run dev -w @infixer/polyfill-web-haptics-api",
    "build": "npm run build -w @infixer/polyfill-web-haptics-api && npm run build -w web",
    "preview": "npm run preview -w web",
    "check": "npm run typecheck -w @infixer/polyfill-web-haptics-api && npm run check -w web",
    "deploy": "npm run build && npm run deploy -w web"
  }
}
